<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="head flex justify-center">
            <h1 class="text-blue-600 pt-4 text-3xl font-bold">Task Manager</h1>
        </div>
        <div class="form text-white p-10">
            <form action="/create" method="post">
                <input class="w-full block bg-zinc-800 rounded-md px-3 py-3 outline-none" type="text"
                    placeholder="Enter title of the task" name="title">
                <textarea class="w-full mt-5 px-3 py-4 block bg-zinc-800 rounded-md outline-none resize-none"
                    placeholder="Enter the content of your task" name="content"></textarea>
                <button class="block bg-blue-500 px-3 py-3 mt-4 rounded-md cursor-pointer" type="submit">Create
                    Post</button>
            </form>
        </div>
        <div class="task text-white p-10 flex gap-5 flex-wrap">

            <% if( files.length > 0) { %>
            <% files.map((value) => { %>
            <div class="card bg-zinc-800 min-w-72 px-3 py-4 rounded-md">
                <h1 class="text-2xl tracking-tighter"><%= value %></h1>
                <div class="flex justify-between items-center">
                    <a href="../files/<%= value %>" class=" text-blue-500 inline-block mt-4">Read more</a>
                    <a href="../edit/<%= value %>" class=" text-zinc-400 inline-block mt-4">Edit filename</a>
                </div>

            </div>
            <% }) %>
            <%} else { %>
            <h1 class="text-zinc-500">No task available</h1>

            <% } %>
        </div>
    </div>
</body>

</html>